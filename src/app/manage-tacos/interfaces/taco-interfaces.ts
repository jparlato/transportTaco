export interface ShellType {
  code: string;
  description: string;
}

export interface ProteinType {
  code: string;
  description: string;
}

export interface ToppingType {
  code: string;
  description: string;
}
